let str = "Quality assurance testing is fun!";
let word = "testing";

if (str.includes(word)) 
{
    console.log(`The string contains "${word}"`);
} 
else 
{
    console.log(`The string does not contain "${word}"`);
}
