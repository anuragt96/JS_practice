let str = "automation";
let startChar = "a";
let endChar = "n";

if (str.startsWith(startChar) && str.endsWith(endChar))     
{
    console.log("The string starts and ends with the given characters.");
} 
else 
{
    console.log("The string does not start and end with those characters.");
}
