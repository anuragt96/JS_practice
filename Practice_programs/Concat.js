const prompt = require('prompt-sync')();

let f_name = prompt("Enter the first name : ");
let l_name = prompt("Enter the last name : ");

 console.log("Concatination of the names are : " + f_name + " " +l_name);
 
